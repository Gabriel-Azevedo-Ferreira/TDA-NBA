Parameters are defind as follows:

	overlaps <- c(5,15,30,40,50,60)  
	num_Intervals_array <- c(10,20,25,30,35,40)
	num_bins_array <- c(2,4,6,8,10,12)

Overlaps: different files
Intervals : Rows 
num_bins : Columns

other changes concern the metric (var normalized or unit normilized (all lines having length 1)) and the data - divided or not by minutes played